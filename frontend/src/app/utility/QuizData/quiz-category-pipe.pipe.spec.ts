import { QuizCategoryPipePipe } from './quiz-category-pipe.pipe';

describe('QuizCategoryPipePipe', () => {
  it('create an instance', () => {
    const pipe = new QuizCategoryPipePipe();
    expect(pipe).toBeTruthy();
  });
});
