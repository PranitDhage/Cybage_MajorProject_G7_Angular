import { QuizSearchPipePipe } from './quiz-search-pipe.pipe';

describe('QuizSearchPipePipe', () => {
  it('create an instance', () => {
    const pipe = new QuizSearchPipePipe();
    expect(pipe).toBeTruthy();
  });
});
